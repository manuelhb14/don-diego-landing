import type { CSSProperties } from "react";

export const residentialSectionHeadingStyle = {
    fontFamily: "var(--font-serif)",
    fontSize: "clamp(2.3rem, 3.8vw, 4rem)",
    lineHeight: 1.04,
    letterSpacing: "0",
} satisfies CSSProperties;
